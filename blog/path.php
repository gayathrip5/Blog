<?php 
//  <!-- rootpath constant is to stop error due to stepping out of folder..tootpath provides path to parent folder and check for required one -->
define('ROOT_PATH', realpath(dirname(__FILE__)));
// var_dump(ROOT_PATH);
define('BASE_URL', "http://localhost/blog" );
// we connect file using links ex register and login pages are linked to each other
 
?>